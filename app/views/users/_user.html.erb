<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
<!--  my code-->
  <% if user.admin %>
    <p> Admin rank</p>
  <% end %>
<!--  administrative delete link-->
  <% if current_user.admin? && !current_user?(user) %>
    <div><%= link_to "Delete", user, method: :delete, data: { confirm: "Are You sure?"} %></div>
  <% end %>

</li>